'use strict';

/**
 * animation-library service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::animation-library.animation-library');
